# deploy/stack.env 로 복사한 뒤 값 채우기 (Git 에 올리지 마세요)

# docker compose 가 db 서비스 비번 치환에 씀 — stack.env 에 넣고
# docker compose -f docker-compose.stack.yml --env-file deploy/stack.env ...
POSTGRES_PASSWORD=classpage1234

JWT_SECRET=긴-랜덤-문자열로-바꾸세요
NEIS_API_KEY=
NEIS_OFFICE_CODE=B10
NEIS_SCHOOL_CODE=

# 같은 도메인에서 /api 로만 호출하면 CORS 는 크게 문제 없음 (비워도 됨)
# CORS_ORIGIN=

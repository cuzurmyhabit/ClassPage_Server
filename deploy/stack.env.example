# deploy/stack.env 로 복사한 뒤 값 채우기 (이 파일은 Git에 올리지 마세요)

JWT_SECRET=긴-랜덤-문자열로-바꾸세요
NEIS_API_KEY=
NEIS_OFFICE_CODE=B10
NEIS_SCHOOL_CODE=

# 같은 도메인에서 /api 로만 호출하면 CORS 는 크게 문제 없음 (비워도 됨)
# CORS_ORIGIN=

# 데모 계정 시드(선택): 컨테이너 안에서 실행하려면 이미지에 시드 스크립트가 있어야 함.
# 초기 관리자는 브라우저에서 POST /api/auth/bootstrap-admin (사용자 0명일 때만)
